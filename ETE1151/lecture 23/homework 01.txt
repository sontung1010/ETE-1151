write 2 functions

void swapByReference(int &a, int &b) {
	// your code goes here
}

void swapByPointer(int *aPtr, int *bPtr) {
	// your code goes here
}

In your main() {

declare variables
Enter a and b from the user 	5 10
print a and b to the screen a = 5, b = 10
swapByReference(a, b);
print a and b: 	After the call to swapByReference: a = 10, b = 5

Enter a and b from the user 	50 100
print a and b to the screen a = 50, b = 100
swapByPointer(&a, &b);
print a and b: After the call to swapByPointer: a = 100, b = 50